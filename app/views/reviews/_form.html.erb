<div class="ro">
  <%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: Review::RATINGS %>
  <%= f.submit 'Submit review', class: 'btn-elegant' %>
  <% end %>
</div>
